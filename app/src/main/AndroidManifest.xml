<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid19">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="com.androidtutorialpoint.mymapsappsdirection.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION " />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/epied"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/epied_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MessagingActivity"></activity>
        <activity
            android:name=".Activities.ChatActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activities.HospitalBottomNavigation"
            android:label="@string/title_activity_hospital_bottom_navigation"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity
            android:name=".Activities.HospitalHomeActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity android:name=".Activities.RegisterLikeHospital" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Activities.TreatmentWeb" />
        <activity android:name=".Activities.MaskWeb" />
        <activity
            android:name=".Activities.Prevent_Activity"
            android:label="@string/title_activity_prevent_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.Transmitted_Activity"
            android:label="@string/title_activity_transmitted_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ScrollingSymptom"
            android:label="@string/title_activity_scrolling_symptom"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.NearbyClinicLocation"
            android:label="@string/title_activity_nearby_clinic_location" />
        <activity android:name=".Activities.Local_News" />
        <activity android:name=".Activities.OfficialDetail" />
        <activity android:name=".Activities.NewsDetailActivity" /> <!-- <activity android:name=".FirstOpening" /> -->
        <!-- <activity android:name=".Activities.MainActivity"> -->
        <!-- &lt;!&ndash; <intent-filter> &ndash;&gt; -->
        <!-- &lt;!&ndash; <action android:name="android.intent.action.MAIN" /> &ndash;&gt; -->
        <!-- &lt;!&ndash; <category android:name="android.intent.category.LAUNCHER" /> &ndash;&gt; -->
        <!-- &lt;!&ndash; </intent-filter> &ndash;&gt; -->
        <!-- </activity> -->
        <activity android:name=".Activities.IntroActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Opening" />
        <activity android:name=".Activities.Login">

        </activity>
        <activity android:name=".Activities.UserProfile" />
        <activity android:name=".FirstOpening" /> <!-- <activity android:name=".FirstOpening" /> -->
        <activity
            android:name=".Activities.Home"
            android:label="@string/title_activity_home" />
        <activity android:name=".Activities.DetailActivity" />
        <activity android:name=".Activities.TrackCountry" />
        <activity android:name=".Activities.InformationActivity" />
        <activity
            android:name=".Activities.NewsActivity"
            android:label="@string/title_activity_news" />
        <activity android:name=".Activities.BottomActivity" />
        <activity
            android:name=".Activities.BottomBar"
            android:label="@string/title_activity_bottom_bar"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>